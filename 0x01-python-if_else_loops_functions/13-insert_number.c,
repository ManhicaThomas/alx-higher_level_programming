#include "lists.h"

/**
* @insert_node - this inserts a num into a sorted-linked list
* @head: pointer the head of the linked list
* @number: num to insert
* Return: 0 if the funct fails or pointer to the new node
*/
listint_t *insert_node(listint_t **head, int number)
{

